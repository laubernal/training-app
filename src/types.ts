export type ObjectDefinition = { [key: string]: any };

export type queryResultTraining = {
  tr_id: string;
  tr_date: string;
  tr_note: string;
  fk_us_id: string;
  tr_title: string;
  tr_ex_id: string;
  training_id: string;
  exercise_id: string;
  ex_id: string;
  ex_name: string;
  fk_cat_id: string;
  set_id: string;
  set_count: number;
  set_reps: number;
  set_weight: string;
  fk_ex_id: string;
  cat_id: string;
  cat_name: string;
};
